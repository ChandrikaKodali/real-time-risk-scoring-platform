apiVersion: apps/v1
kind: Deployment
metadata:
  name: risk-zookeeper
spec:
  replicas: 1
  selector:
    matchLabels:
      app: risk-zookeeper
  template:
    metadata:
      labels:
        app: risk-zookeeper
    spec:
      containers:
        - name: zookeeper
          image: confluentinc/cp-zookeeper:7.7.1
          ports:
            - containerPort: 2181
          env:
            - name: ZOOKEEPER_CLIENT_PORT
              value: "2181"
            - name: ZOOKEEPER_TICK_TIME
              value: "2000"

---
apiVersion: v1
kind: Service
metadata:
  name: risk-zookeeper
spec:
  selector:
    app: risk-zookeeper
  ports:
    - port: 2181
      targetPort: 2181